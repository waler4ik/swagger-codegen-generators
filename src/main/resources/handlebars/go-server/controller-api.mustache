{{>partial_header}}
package {{apiPackage}}

{{#operations}}
import (
	"net/http"
)

type {{classname}}Handler interface {
	{{#operation}}
	{{nickname}}(w http.ResponseWriter, r *http.Request)
	{{/operation}}
}
{{/operations}}
